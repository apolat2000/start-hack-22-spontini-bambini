import 'package:flutter/material.dart';
import 'package:oktoast/oktoast.dart';

void notImplemented() {
  showToast("This action is not avalible in the current version",
      position: ToastPosition.bottom);
}
