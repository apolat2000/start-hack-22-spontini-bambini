const corsConfig = require("./cors.config");
const serverConfig = require("./server.config");
const databaseConfig = require("./database.config");
const cryptConfig = require("./crypt.config");

module.exports = {
  corsConfig,
  serverConfig,
  databaseConfig,
  cryptConfig,
};
